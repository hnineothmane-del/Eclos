{
  "name": "ai-rival-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/web",
    "packages/domain"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/domain && npm run build --workspace=apps/web",
    "test": "npm run test --workspace=packages/domain && npm run test --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=packages/domain && npm run typecheck --workspace=apps/web",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "globals": "^15.9.0",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.2.0",
    "vitest": "^2.0.5"
  }
}
